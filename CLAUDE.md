# HMI 项目维护规则

## 1. 定位与目标

本项目只维护 Orange VisualTFT 串口屏工程，独立于 ARM、STM32 和 Windows 仓。学习目标是理解页面、变量、串口协议和导出产物的关系；维护目标是稳定修改、导出、回退并让真屏加载正确版本。

## 2. 目录与边界

- `source/`：唯一 VisualTFT 工程编辑区。
- `build/`：预览和临时导出目录。
- `artifacts/`：`.tft`、`.gwb`、`.si` 等导出文件。
- `evidence/`：预览图、导出日志、产物哈希和真屏回读记录。
- `docs/`：页面地图、变量/协议表、交互状态和维护教程。
- `tools/`：同步和辅助脚本。

导出文件不能替代工程源文件，也不能因文件名正确就宣称真屏已生效。

## 3. 基线与 GUI 构建

- 当前交接基线：tag `handover-20260922-r1`。
- VisualTFT GUI 编译、导出和真屏下载由用户执行；维护者准备源文件、版本说明、预期产物和回读方法。
- 每次交付记录 VisualTFT 版本、工程 full commit、屏型号、产物 SHA256、下载入口和真屏验证结果。

## 4. 学习与迭代

`docs/` 至少维护页面层级图、页面跳转和状态机、变量与串口字段表、关键控件/脚本说明、异常显示和恢复路径。说明写清输入、输出、触发条件、失败表现和可改点；不做逐控件流水账。

先在源码工程中修改并保留可回退提交，再由用户执行 GUI 导出。协议字段、单位、故障显示和默认值变更必须同时核对 STM32/ARM/Windows 影响。预览通过不等于真屏下载成功。

## 5. 验收与回退

分别标记源码审查、GUI 导出、产物身份、预览、真屏回读和现场效果。回退使用上一个已标记 tag/commit 及其对应导出物，不用未绑定来源的同名文件覆盖。
